[
  {
    "path": "bright_memory_infinite_trainer/__init__.py",
    "content": "#!/usr/bin/env python3\n\"\"\"\nBright Memory: Infinite Trainer\nA memory editing trainer for the game Bright Memory: Infinite.\nProvides features like infinite health, ammo, and super speed.\n\"\"\"\n\n__version__ = \"1.0.0\"\n__author__ = \"Trainer Dev\"\n"
  },
  {
    "path": "bright_memory_infinite_trainer/memory_scanner.py",
    "content": "#!/usr/bin/env python3\n\"\"\"\nMemory scanner module for Bright Memory: Infinite Trainer.\nUses pymem to read and write process memory.\n\"\"\"\n\nimport pymem\nimport pymem.process\nfrom typing import Optional, List, Tuple\n\n\nclass MemoryScanner:\n    \"\"\"Handles memory scanning and editing for the target process.\"\"\"\n\n    def __init__(self, process_name: str = \"BrightMemoryInfinite-Win64-Shipping.exe\"):\n        \"\"\"\n        Initialize the memory scanner.\n        \n        Args:\n            process_name: The executable name of the game.\n        \"\"\"\n        self.process_name = process_name\n        self.pm: Optional[pymem.Pymem] = None\n        self.process_handle = None\n\n    def attach_to_process(self) -> bool:\n        \"\"\"\n        Attach to the game process.\n        \n        Returns:\n            True if successful, False otherwise.\n        \"\"\"\n        try:\n            self.pm = pymem.Pymem(self.process_name)\n            self.process_handle = self.pm.process_handle\n            return True\n        except pymem.exception.ProcessNotFound:\n            print(f\"Error: {self.process_name} not found. Make sure the game is running.\")\n            return False\n        except Exception as e:\n            print(f\"Error attaching to process: {e}\")\n            return False\n\n    def read_int(self, address: int) -> Optional[int]:\n        \"\"\"\n        Read an integer from a memory address.\n        \n        Args:\n            address: Memory address to read from.\n        \n        Returns:\n            The integer value, or None on error.\n        \"\"\"\n        try:\n            return self.pm.read_int(address)\n        except Exception as e:\n            print(f\"Error reading int at {hex(address)}: {e}\")\n            return None\n\n    def write_int(self, address: int, value: int) -> bool:\n        \"\"\"\n        Write an integer to a memory address.\n        \n        Args:\n            address: Memory address to write to.\n            value: The integer value to write.\n        \n        Returns:\n            True if successful, False otherwise.\n        \"\"\"\n        try:\n            self.pm.write_int(address, value)\n            return True\n        except Exception as e:\n            print(f\"Error writing int at {hex(address)}: {e}\")\n            return False\n\n    def read_float(self, address: int) -> Optional[float]:\n        \"\"\"\n        Read a float from a memory address.\n        \n        Args:\n            address: Memory address to read from.\n        \n        Returns:\n            The float value, or None on error.\n        \"\"\"\n        try:\n            return self.pm.read_float(address)\n        except Exception as e:\n            print(f\"Error reading float at {hex(address)}: {e}\")\n            return None\n\n    def write_float(self, address: int, value: float) -> bool:\n        \"\"\"\n        Write a float to a memory address.\n        \n        Args:\n            address: Memory address to write to.\n            value: The float value to write.\n        \n        Returns:\n            True if successful, False otherwise.\n        \"\"\"\n        try:\n            self.pm.write_float(address, value)\n            return True\n        except Exception as e:\n            print(f\"Error writing float at {hex(address)}: {e}\")\n            return False\n\n    def scan_for_value(self, value: int, value_type: str = \"int\") -> List[int]:\n        \"\"\"\n        Scan the process memory for a specific value.\n        \n        Args:\n            value: The value to search for.\n            value_type: Type of value (\"int\" or \"float\").\n        \n        Returns:\n            List of memory addresses where the value is found.\n        \"\"\"\n        try:\n            if value_type == \"int\":\n                return pymem.pattern.scan_all(self.pm, value.to_bytes(4, 'little'))\n            elif value_type == \"float\":\n                import struct\n                return pymem.pattern.scan_all(self.pm, struct.pack('<f', value))\n            else:\n                print(f\"Unsupported value type: {value_type}\")\n                return []\n        except Exception as e:\n            print(f\"Error scanning for value: {e}\")\n            return []\n\n    def get_base_address(self) -> Optional[int]:\n        \"\"\"\n        Get the base address of the main module.\n        \n        Returns:\n            Base address, or None if not found.\n        \"\"\"\n        try:\n            return pymem.process.module_from_name(self.pm.process_handle, self.process_name).lpBaseOfDll\n        except Exception as e:\n            print(f\"Error getting base address: {e}\")\n            return None\n\n    def detach(self) -> None:\n        \"\"\"Detach from the process.\"\"\"\n        if self.pm:\n            self.pm.close_process()\n            self.pm = None\n            self.process_handle = None\n"
  },
  {
    "path": "bright_memory_infinite_trainer/trainer.py",
    "content": "#!/usr/bin/env python3\n\"\"\"\nMain trainer module for Bright Memory: Infinite.\nImplements specific cheats using memory addresses.\n\"\"\"\n\nfrom .memory_scanner import MemoryScanner\nfrom typing import Dict, Any, Optional\nimport time\n\n\nclass BrightMemoryInfiniteTrainer:\n    \"\"\"\n    Trainer class with feature toggles for Bright Memory: Infinite.\n    Uses known memory offsets for player stats.\n    \"\"\"\n\n    # Known offsets (relative to base address) for game version 1.0\n    # These are example offsets and would need to be updated for actual game versions.\n    OFFSETS = {\n        \"health\": 0x00A1B2C0,      # Player health (int)\n        \"max_health\": 0x00A1B2C4,  # Max health (int)\n        \"ammo\": 0x00B3C4D0,       # Current ammo (int)\n        \"max_ammo\": 0x00B3C4D4,   # Max ammo (int)\n        \"speed\": 0x00C5D6E0,      # Movement speed (float)\n        \"score\": 0x00D7E8F0,      # Score (int)\n    }\n\n    def __init__(self):\n        \"\"\"Initialize the trainer.\"\"\"\n        self.scanner = MemoryScanner()\n        self.base_address: Optional[int] = None\n        self.features = {\n            \"infinite_health\": False,\n            \"infinite_ammo\": False,\n            \"super_speed\": False,\n        }\n        self._original_speed: Optional[float] = None\n        self._running = False\n\n    def attach(self) -> bool:\n        \"\"\"\n        Attach to the game process.\n        \n        Returns:\n            True if successful.\n        \"\"\"\n        if not self.scanner.attach_to_process():\n            return False\n        self.base_address = self.scanner.get_base_address()\n        if self.base_address is None:\n            print(\"Failed to get base address.\")\n            self.scanner.detach()\n            return False\n        print(f\"Attached to process. Base address: {hex(self.base_address)}\")\n        return True\n\n    def _get_absolute_address(self, offset: int) -> int:\n        \"\"\"\n        Calculate absolute memory address from base + offset.\n        \n        Args:\n            offset: Memory offset from base.\n        \n        Returns:\n            Absolute memory address.\n        \"\"\"\n        if self.base_address is None:\n            raise RuntimeError(\"Not attached to process.\")\n        return self.base_address + offset\n\n    def set_infinite_health(self, enabled: bool) -> None:\n        \"\"\"\n        Toggle infinite health.\n        \n        Args:\n            enabled: True to enable, False to disable.\n        \"\"\"\n        self.features[\"infinite_health\"] = enabled\n        if enabled:\n            addr = self._get_absolute_address(self.OFFSETS[\"health\"])\n            max_addr = self._get_absolute_address(self.OFFSETS[\"max_health\"])\n            max_hp = self.scanner.read_int(max_addr)\n            if max_hp:\n                self.scanner.write_int(addr, max_hp)\n                print(\"Infinite health enabled.\")\n        else:\n            print(\"Infinite health disabled.\")\n\n    def set_infinite_ammo(self, enabled: bool) -> None:\n        \"\"\"\n        Toggle infinite ammo.\n        \n        Args:\n            enabled: True to enable, False to disable.\n        \"\"\"\n        self.features[\"infinite_ammo\"] = enabled\n        if enabled:\n            addr = self._get_absolute_address(self.OFFSETS[\"ammo\"])\n            max_addr = self._get_absolute_address(self.OFFSETS[\"max_ammo\"])\n            max_ammo = self.scanner.read_int(max_addr)\n            if max_ammo:\n                self.scanner.write_int(addr, max_ammo)\n                print(\"Infinite ammo enabled.\")\n        else:\n            print(\"Infinite ammo disabled.\")\n\n    def set_super_speed(self, enabled: bool, speed_multiplier: float = 2.0) -> None:\n        \"\"\"\n        Toggle super speed (multiplies movement speed).\n        \n        Args:\n            enabled: True to enable, False to disable.\n            speed_multiplier: Speed multiplier when enabled.\n        \"\"\"\n        addr = self._get_absolute_address(self.OFFSETS[\"speed\"])\n        if enabled:\n            if self._original_speed is None:\n                self._original_speed = self.scanner.read_float(addr)\n            if self._original_speed:\n                self.scanner.write_float(addr, self._original_speed * speed_multiplier)\n                self.features[\"super_speed\"] = True\n                print(f\"Super speed enabled (x{speed_multiplier}).\")\n        else:\n            if self._original_speed is not None:\n                self.scanner.write_float(addr, self._original_speed)\n            self.features[\"super_speed\"] = False\n            print(\"Super speed disabled.\")\n\n    def get_status(self) -> Dict[str, Any]:\n        \"\"\"\n        Get the current status of all features.\n        \n        Returns:\n            Dictionary with feature status and memory values.\n        \"\"\"\n        status = {\n            \"attached\": self.base_address is not None,\n            \"features\": self.features.copy(),\n        }\n        if self.base_address:\n            try:\n                hp_addr = self._get_absolute_address(self.OFFSETS[\"health\"])\n                ammo_addr = self._get_absolute_address(self.OFFSETS[\"ammo\"])\n                speed_addr = self._get_absolute_address(self.OFFSETS[\"speed\"])\n                status[\"current_health\"] = self.scanner.read_int(hp_addr)\n                status[\"current_ammo\"] = self.scanner.read_int(ammo_addr)\n                status[\"current_speed\"] = self.scanner.read_float(speed_addr)\n            except Exception as e:\n                status[\"error\"] = str(e)\n        return status\n\n    def run_loop(self, interval: float = 0.1) -> None:\n        \"\"\"\n        Run the trainer loop, continuously applying cheats.\n        \n        Args:\n            interval: Time between updates in seconds.\n        \"\"\"\n        if not self.attach():\n            return\n        self._running = True\n        print(\"Trainer running. Press Ctrl+C to stop.\")\n        try:\n            while self._running:\n                if self.features[\"infinite_health\"]:\n                    self.set_infinite_health(True)\n                if self.features[\"infinite_ammo\"]:\n                    self.set_infinite_ammo(True)\n                time.sleep(interval)\n        except KeyboardInterrupt:\n            print(\"\\nStopping trainer...\")\n        finally:\n            self.cleanup()\n\n    def cleanup(self) -> None:\n        \"\"\"Clean up and restore original values.\"\"\"\n        self._running = False\n        if self.features[\"super_speed\"]:\n            self.set_super_speed(False)\n        self.scanner.detach()\n        print(\"Trainer detached.\")\n\n    def __enter__(self):\n        self.attach()\n        return self\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        self.cleanup()\n"
  },
  {
    "path": "bright_memory_infinite_trainer/cli.py",
    "content": "#!/usr/bin/env python3\n\"\"\"\nCommand-line interface for Bright Memory: Infinite Trainer.\n\"\"\"\n\nfrom .trainer import BrightMemoryInfiniteTrainer\nimport argparse\nimport sys\n\n\ndef main():\n    \"\"\"Main entry point for the CLI.\"\"\"\n    parser = argparse.ArgumentParser(\n        description=\"Bright Memory: Infinite Trainer - Memory editing trainer\"\n    )\n    parser.add_argument(\n        \"--health\",\n        action=\"store_true\",\n        help=\"Enable infinite health\"\n    )\n    parser.add_argument(\n        \"--ammo\",\n        action=\"store_true\",\n        help=\"Enable infinite ammo\"\n    )\n    parser.add_argument(\n        \"--speed\",\n        type=float,\n        nargs=\"?\",\n        const=2.0,\n        metavar=\"MULTIPLIER\",\n        help=\"Enable super speed (optional multiplier, default: 2.0)\"\n    )\n    parser.add_argument(\n        \"--status\",\n        action=\"store_true\",\n        help=\"Print current status and exit\"\n    )\n    parser.add_argument(\n        \"--loop\",\n        action=\"store_true\",\n        help=\"Run continuous loop until interrupted\"\n    )\n\n    args = parser.parse_args()\n\n    trainer = BrightMemoryInfiniteTrainer()\n\n    if args.status:\n        if trainer.attach():\n            status = trainer.get_status()\n            print(\"Trainer Status:\")\n            for key, value in status.items():\n                print(f\"  {key}: {value}\")\n            trainer.cleanup()\n        else:\n            print(\"Failed to attach to game.\")\n            sys.exit(1)\n        return\n\n    if args.loop:\n        trainer.run_loop()\n        return\n\n    # One-time application mode\n    if not trainer.attach():\n        print(\"Failed to attach to game.\")\n        sys.exit(1)\n\n    if args.health:\n        trainer.set_infinite_health(True)\n    if args.ammo:\n        trainer.set_infinite_ammo(True)\n    if args.speed is not None:\n        trainer.set_super_speed(True, args.speed)\n\n    # Print final status\n    status = trainer.get_status()\n    print(\"\\nFinal Status:\")\n    for key, value in status.items():\n        print(f\"  {key}: {value}\")\n\n    trainer.cleanup()\n\n\nif __name__ == \"__main__\":\n    main()\n"
  },
  {
    "path": "requirements.txt",
    "content": "pymem>=1.13.0\n"
  },
  {
    "path": "tests/__init__.py",
    "content": "#!/usr/bin/env python3\n\"\"\"\nTests for Bright Memory: Infinite Trainer.\n\"\"\"\n"
  },
  {
    "path": "tests/test_trainer.py",
    "content": "#!/usr/bin/env python3\n\"\"\"\nUnit tests for the trainer module.\nUses mocking to avoid needing the actual game process.\n\"\"\"\n\nimport unittest\nfrom unittest.mock import patch, MagicMock\nfrom bright_memory_infinite_trainer.trainer import BrightMemoryInfiniteTrainer\nfrom bright_memory_infinite_trainer.memory_scanner import
